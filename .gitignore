# Build output (regenerated by build.sh)
.build/
dist/

# macOS cruft
.DS_Store

# Generated icon/preview artifacts
design/menubar_icon_preview.png

# Never commit secrets — the Google OAuth JSON lives outside the repo.
# This is a guard against an accidental copy.
*oauth*.json
*secret*.json

# Lokale Signatur-Konfiguration (Zertifikatsname + Fingerabdruck)
signing.local
